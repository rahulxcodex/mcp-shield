# Releasing

This document outlines the process for releasing new versions of MCP-Shield.

## Process
1. Update version in `package.json`
2. Create a new GitHub release
3. Publish to npm via CI/CD (OIDC trusted publishing)
